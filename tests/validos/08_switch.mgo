package main;

func main() {
    var x int = 2;

    switch x {
        case 1:
            println(1);
        case 2:
            println(2);
        default:
            println(0);
    };

    switch {
        case x < 10:
            println(`menor`);
        default:
            println(`mayor`);
    };
};
